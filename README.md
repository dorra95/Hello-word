# Hello-word

## Setup (VS Code)

```bash
python -m venv .venv
# Windows:  .venv\Scripts\activate
# macOS/Linux:  source .venv/bin/activate
pip install -r requirements-notebook.txt
```

Open `hello.ipynb` in VS Code and select the `.venv` interpreter as the kernel.
